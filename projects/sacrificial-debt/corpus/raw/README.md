# Raw Sources

Raw source binaries/text are ignored by default. Commit only materials whose redistribution status permits it. Every local source must still be registered with provenance and SHA-256 metadata so another researcher can identify the exact source used.
